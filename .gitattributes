# Always check out Linux artefacts with LF line endings, even on Windows.
# Prevents bash "\r": command not found errors when scripts are piped over SSH.

*.sh    text eol=lf
*.conf  text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf

# Default text handling
*       text=auto
